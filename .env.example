# Required: Your Anthropic API key.
# Get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=your-api-key-here

# Optional: Disable cross-run plan caching
# DISPATCH_DISABLE_CACHE=1

# Optional: Custom cache directory (default: .dispatch-cache)
# DISPATCH_CACHE_DIR=/path/to/cache
